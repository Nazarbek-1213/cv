"""Reserved for future custom forms.

The CV is fully editable through Django's built-in admin (`/admin/`), which is
reachable from the site by typing the secret trigger word (default: `safarov2`)
anywhere on the page.
"""
